// onConnect / onDisconnect

import { UWSReq, UWSRes } from "@/types/type.uws";

// Todo Write Connection Socket here
export async function onConnect(req:UWSReq, res:UWSRes) {
    
}

// onMessageSent (broadcast to chat members)

// onMessageRead (send read receipts)

// onTyping (typing indicators)

// onUserStatusChange (online/offline presence)
